extends layout

block content
  h1= title
  p Welcome to #{title}
  br
  br
  form(action='/delete/delete_mountain' method='post')
    fieldset
      .form-group.row
        label.col-sm-1.col-form-label(for='poiname') POI Name
        .col-sm-4
          input#name.form-control(type='text' name="poiname")
      button.btn.btn-primary.mb-2(type='submit') Löschen