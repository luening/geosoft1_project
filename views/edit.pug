extends layout

block content
  h1= title
  p Um ein Gebrige zu bearbeiten oder zu löschen, müssen Sie zuerst ein Gebirge auf der Karte auswählen. Dann können Sie Attribute ihrer Wahl ändern und das Gebrige dann speichern oder es direkt löschen. 
  p Achtung: Erst wenn Sie Speichern klicken, werden die veränderten Attribute übernommen.
  br
  #map
  form(action='/edit/update_mountain' method='post')
    fieldset
      .form-group.row
        label.col-form-label(for='id') ID
        .col-sm-4
          input#id.form-control(type='text' name="id" readonly)
      .form-group.row
        label.col-form-label(for='mountain') Name
        .col-sm-4
          input#mountain.form-control(type='text' name="mountain")
      .form-group.row
        label.col-form-label(for='altitude') Höhe
        .col-sm-4
          input#altitude.form-control(type='number' name="altitude")
      .form-group.row
        label.col-form-label(for='url') URL
        .col-sm-4
          input#url.form-control(type='text' name="url")
      .form-group.row
        label.col-form-label(for='description') Beschreibung
        .col-sm-4
          input#description.form-control(type='text' name="description" readonly)
      .form-group.row
        label.col-form-label(for='long') Longitude
        .col-sm-4
          input#long.form-control(type='text' name="long")
      .form-group.row
        label.col-form-label(for='lat') Latitude
        .col-sm-4
          input#lat.form-control(type='text' name="lat")
      button.btn.btn-primary.mb-2(type='submit' value="speichern") Speichern
      button.btn.btn-primary.mb-2(type='submit' formaction="/edit/delete_mountain") Löschen



  script. 
    let geojson = !{JSON.stringify(data)}

  block scripts 
    script(src="/javascripts/edit_mountain.js")